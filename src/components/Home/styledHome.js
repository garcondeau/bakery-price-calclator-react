import styled from 'styled-components';
import {StyledContainer} from "../elements/container/styledContainer";

export const StyledHomeContainer = styled(StyledContainer)`
    flex-direction: column;
`;